# UVDM API Server Configuration

# SECURITY WARNING: This file contains default values for testing/development only.
# For production use:
# 1. Generate a secure admin key (32+ characters, random)
#    Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 2. Use environment variables or a secure secrets manager instead of this file
# 3. Never commit real credentials to version control

# Server host and port
UVDM_API_HOST=0.0.0.0
UVDM_API_PORT=5000
UVDM_API_DEBUG=False

# Admin key for license generation
# ⚠️ CHANGE THIS IMMEDIATELY IN PRODUCTION! ⚠️
# Default value 'admin123' is for testing ONLY
UVDM_ADMIN_KEY=admin123

# License server URL (used by clients)
UVDM_LICENSE_SERVER=http://localhost:5000
